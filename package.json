{
  "name": "with-react-esi",
  "author": "Kévin Dunglas <dunglas@gmail.com>",
  "main": "dist/server.js",
  "dependencies": {
    "express": "^4.17.1",
    "next": "latest",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-esi": "^0.3.0"
  },
  "scripts": {
    "build": "yarn install && babel src -d dist && next build dist",
    "start": "NODE_ENV=production node dist/server.js"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/node": "^7.2.2"
  }
}
